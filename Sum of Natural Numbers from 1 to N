#include <iostream>
using namespace std;

int main()
{
    int N, sum = 0;

    cout << "Enter the value of N: ";
    cin >> N;

    for (int i = 1; i <= N; i++)
    {
        sum = sum + i;
    }

    cout << "Sum of natural numbers from 1 to " << N << " = " << sum << endl;

    return 0;
}
